import { MapItemPositionDirective } from './map-item-position.directive';

describe('MapItemPositionDirective', () => {
  it('should create an instance', () => {
    const directive = new MapItemPositionDirective();
    expect(directive).toBeTruthy();
  });
});
