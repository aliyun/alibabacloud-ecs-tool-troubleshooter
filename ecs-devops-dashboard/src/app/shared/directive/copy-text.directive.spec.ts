import { CopyTextDirective } from './copy-text.directive';

describe('CopyTextDirective', () => {
  it('should create an instance', () => {
    const directive = new CopyTextDirective();
    expect(directive).toBeTruthy();
  });
});
