import { EmptyEchartsDirective } from './empty-echarts.directive';

describe('EmptyEchartsDirective', () => {
  it('should create an instance', () => {
    const directive = new EmptyEchartsDirective();
    expect(directive).toBeTruthy();
  });
});
