import { InstanceMaintenanceAttrPipe } from './instance-maintenance-attr.pipe';

describe('InstanceMaintenanceAttrPipe', () => {
  it('create an instance', () => {
    const pipe = new InstanceMaintenanceAttrPipe();
    expect(pipe).toBeTruthy();
  });
});
