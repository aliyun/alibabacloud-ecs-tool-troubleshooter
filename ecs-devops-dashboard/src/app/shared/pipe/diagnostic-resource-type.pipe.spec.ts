import { DiagnosticResourceTypePipe } from './diagnostic-resource-type.pipe';

describe('DiagnosticResourceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new DiagnosticResourceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
