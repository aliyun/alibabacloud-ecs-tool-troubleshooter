import { DiagnosticReportStatusPipe } from './diagnostic-report-status.pipe';

describe('DiagnosticReportStatusPipe', () => {
  it('create an instance', () => {
    const pipe = new DiagnosticReportStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
